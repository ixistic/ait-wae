<h1>Web08 Team's Homepage</h1>
<p>How a divide-by-zero exception is generated:</p>
<ol>
  <li>@result = 1/0 is created in a controller (basics_controller.rb)</li>
  <li>@result is then displayed in a view (exception.html.erb)</li>
  <li>A link that points to exception.html.erb is placed on the homepage.
    The link will trigger the divide-by-zero exception when clicked.</li>
</ol>
<h3>See <a href="/basics/exception">a divide-by-zero exception</a></h3>
<h3>See <a href="/basics/">the top 10 trending videos</a> on YouTube</h3>
